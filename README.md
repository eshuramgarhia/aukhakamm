# Jenkins Node Project

Simple Node.js application used to test Jenkins CI/CD Pipeline.

## Commands
- Start: `npm start`
- Build: `npm run build`
- Test: `npm test`

## Jenkins Pipeline
This project is configured with a Jenkinsfile for automated build.
